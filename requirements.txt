# Virtual-Smart-Factory 依赖清单（Windows 10 + Python 3.12）
# 原则：只用标准库 + numpy + flask + pymodbus，前端 ECharts 走 CDN 不进依赖。
#
# 班次1 实际使用：numpy（视觉尺寸高斯采样）
# 班次2 预留：flask（SCADA Web 服务）、pymodbus（Modbus TCP 从站）
numpy>=2.0
flask>=3.0
pymodbus>=3.6
